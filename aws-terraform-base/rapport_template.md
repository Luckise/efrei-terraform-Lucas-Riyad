(# Rapport de TP — Modèle

## Titre
- Nom du TP : (ex. Déploiement AWS via Terraform)
- Étudiants : (Noms, groupes)
- Date :

## Résumé
Bref résumé (2-4 phrases) des objectifs et du résultat obtenu.

## Architecture
- Description textuelle de l'architecture déployée.
- Schéma/diagramme : voir `architecture.puml` (PlantUML) ou joindre une image exportée.

## Ressources créées
- Liste synthétique des ressources (VPC, subnets, EC2, S3, IAM, etc.).

## Choix techniques et justifications
- Réseaux (CIDR, segmentation public/privé)
- Types d'instances, stockage, politiques IAM importantes
- Choix de modularisation et réutilisabilité

## Fichiers modifiés
- `main.tf` : (description des changements)
- `variables.tf` : (variables ajoutées/modifiées)
- `outputs.tf` : (outputs exposés)
- `terraform.tfvars` : (valeurs utilisées, sans secrets)

## Procédure de validation
- Tests effectués et résultats (ex. accès SSH, HTTP, permissions S3).
- Résumé des vérifications manuelles ou automatisées.

## Principales sorties
- Joindre `outputs.txt` ou coller les valeurs principales ici.

## Captures et preuves
- Captures d'écran des services accessibles
- Logs pertinents

## Difficultés rencontrées et solutions
- Points bloquants et comment ils ont été résolus.

## Conclusion
- Bilan, améliorations possibles et pistes d'extension.

## Annexes
- Reproduire : préciser si des prérequis ou un backend distant sont nécessaires.
)
