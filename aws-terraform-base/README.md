# AWS Terraform Base

Starter Terraform skeleton for AWS.

## Important

- Never commit AWS credentials or tokens.
- Keep secrets out of `terraform.tfvars` and `*.auto.tfvars`.
- Use AWS CLI or environment variables for authentication.
